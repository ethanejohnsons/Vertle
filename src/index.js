import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import Vertle from './Vertle';

ReactDOM.render(
  <React.StrictMode>
    <Vertle />
  </React.StrictMode>,
  document.getElementById('root')
);
